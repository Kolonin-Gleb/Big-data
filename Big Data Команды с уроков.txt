__________ Big Data ____________
Полезные команды с урока:

SELECT * from hr.employees WHERE
DEPARTMENT_ID not in (60, 30, 100)

SELECT * from hr.employees
WHERE first_name like 'S%';

SELECT * from hr.employees
WHERE first_name like '%ll%';

-------- УРОК 3 -----------------

CREATE TABLE goods
(
    id INT PRIMARY KEY,
    title VARCHAR(30),
    quantity NUMBER,
    in_stock CHAR(1) CHECK (in_stock in ('Y', 'N'))
);

INSERT INTO goods (id, title, quantity, in_stock) values (1, 'bike', 10, 'Y');
INSERT INTO goods (id, title, quantity, in_stock) VALUES (2, 'laptop', 30, 'Y');
INSERT INTO goods (id, title, quantity, in_stock) VALUES (3, 'toy', 160, 'Y');
INSERT INTO goods (id, title, quantity, in_stock) VALUES (4, 'car', 20, 'Y');

UPDATE goods SET quantity = quantity + 150 WHERE title='bike';

select * from goods;

SELECT * FROM goods WHERE quantity < 150;

CREATE VIEW v_goods AS 
SELECT * FROM goods WHERE quantity < 150;


SELECT * FROM v_goods;


CREATE TABLE goods_2 AS SELECT * FROM goods;

select * from goods_2;


DELETE FROM goods WHERE
id = 1;


TRUNCATE TABLE goods; -- Обнулит автоинкремент


-- Изменение таблицы















